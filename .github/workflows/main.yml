name: first workflow
on: push

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: echo "hello World"
      - name: Multiline run-shell-commands
        run: |
         node -v
         npm -v
